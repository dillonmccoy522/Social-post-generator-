{
  "name": "task-bridge",
  "version": "1.0.0",
  "description": "Niewdel Task Bridge — Claude custom connector for Google Tasks + Google Calendar",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^16.4.0",
    "express": "^4.18.0",
    "googleapis": "^173.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
